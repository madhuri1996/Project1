import { PipeTitleCasePipe } from './pipe-title-case.pipe';

describe('PipeTitleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTitleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
